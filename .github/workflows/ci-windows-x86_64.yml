name: windows-x86_64

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  windows-x86_64:
    uses: ./.github/workflows/_build.yml
    with:
      os: windows-latest
